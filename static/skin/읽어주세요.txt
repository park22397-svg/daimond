맨발.png — 양말이 그려지기 전의 살갗 그림

지금은 어디에서도 쓰지 않는다. 벗기는 기능을 빼기로 해서 걷어냈다.
다만 이 그림은 다시 만들 수 없어서 남겨 둔다 —
표현용.vrm 이 맨발이던 판(2026-08-14 21:31)에서 떠낸 것이고,
그 파일은 그 뒤 양말이 그려진 판으로 덮였다.

다시 쓰게 되면 주의할 점이 하나 있다.
그때 살갗이 태닝한 것처럼 어두웠는데, 텍스처 인코딩을 sRGB 로
못박아서였다. 라이브러리(three-vrm)가 MToon 텍스처에 어떤 인코딩을
쓰는지는 판마다 다르므로, 값을 정해 넣지 말고
**그 재질에 이미 붙어 있는 텍스처의 encoding 을 그대로 베껴** 써야 한다.
한 번 더 sRGB -> linear 로 풀리면 그만큼 어두워진다.

  tex.encoding = mat.map.encoding;   // 정해 넣지 말고 베낀다
  tex.flipY = false;                 // glTF 규약
