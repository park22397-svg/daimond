flask>=3.0
requests>=2.31
edge-tts>=6.1

# 올린 데서 계정과 기억을 적어 둘 곳(Vercel Blob).
# 내 컴퓨터에서는 안 쓴다 — store.py 가 파일로 간다.
vercel>=0.10

# 체스 규칙. 캐슬링·앙파상·승격·체크메이트를 손으로 짜면 반드시
# 어딘가 틀린다. 순수 파이썬 565KB 라 올린 데서도 돈다.
chess>=1.11
